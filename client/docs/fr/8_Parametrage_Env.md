# Paramétrage des variables d’environnement — Client

Variables supportées (Vite):
- `VITE_API_BASE_URL` — URL de base de l’API (ex.: `http://localhost:8000`).

Fichier d’exemple:
```
client/.env.example
```
Contenu:
```
VITE_API_BASE_URL=http://localhost:8000
```
